# NaHS 博客写作与排版规范

## 项目概览

个人技术博客，作者为电子信息类专业大二学生「硫氢化钠」（NaHSIT），2023 年 9 月入学。博客内容涵盖嵌入式开发、课程设计、独立项目、学习笔记和校园生活。

博客为纯静态 HTML + JSON 渲染，无框架。文章存储在 `posts/*.md`（Markdown），元数据存储在 `posts.json`，图片存储在 `assets/` 目录。

---

## 文章标题规范

**核心原则：标题命名具体做了什么，不要用课程名称打头。**

### 好的标题（已有文章）

| 模式 | 示例 |
|------|------|
| 基于[平台]的[具体项目]设计 | 基于51单片机的七人表决电路设计 |
| | 基于51单片机的智能红绿灯系统设计 |
| | 基于51单片机的电子万年历设计 |
| 用[工具/技术]做了[具体成果] | 用嘉立创EDA画了一张支持NFC的PCB校园卡 |
| | 用C语言手写哈夫曼编码：从堆排序到文件压缩 |
| | 写了个自动晚点名打卡工具：从忘记打卡到全自动托管 |
| 从[起点]到[终点]：[主题] | 从零搭建分布式机房监控系统：STM32 + ESP32 + OneNET + 全平台应用 |
| | 从100个焊点到0-99计数器：电子线路课设的两块万用板 |
| 零基础：[具体教程主题] | 零基础：在VS Code中开发Keil的STM32工程，并实现代码补全 |
| 生活/随笔类 | 我的第一篇博客：分享生活，记录学习 |
| | 开学季：不仅是代码，还有生活 |

### 禁止的标题

- ❌ `电子线路课程设计` — 这是课程名称，不是文章标题
- ❌ `XX课程设计报告` — 太像实验报告
- ❌ 任何以课程名/学科名直接作为标题的写法

### 标题规则

1. 标题必须包含**具体做了什么**（项目名、技术栈、成果）
2. 课程/课设背景放在正文开头介绍，不要放在标题
3. 技术项目用 `：` 分两段时，前半段抓眼球（做了什么），后半段补充技术细节
4. 标题中英文技术名词保留原文（如 LM358P、NE555、STM32）

---

## 文章结构

### 必备结构（按顺序）

```markdown
# 标题

> 源码：[github.com/...](URL)          ← 有源码的项目必加
> 在线演示：[url](URL)                 ← 有部署的项目必加

开头段落（1-3 段）：
  - 时间背景（大几、什么课、什么场景）
  - 做了什么（一句话说清楚）
  - 个人动机或感受（为什么做、做完什么感觉）

---

## 1. 第一个大段（硬件组成 / 整体架构 / 核心思路）

### 1.1 小段

### 1.2 小段

## 2. 第二个大段（核心逻辑 / 实现细节）

## 3. 踩过的坑 / 调试经历（可选但推荐）

## N. 总结 / 收获 / 感受

> 结语——用 blockquote 写一段个人反思，不要用标题
```

### 结构规则

1. **开头必须交代三个要素**：时间背景（学期/课程）、做了什么、为什么值得写
2. **源码链接紧跟标题**：有开源仓库的项目，第一行 blockquote 放 GitHub 链接
3. **`---` 分割线**：开头引入和正文之间必须用 `---` 隔开
4. **大段用 `##`，小段用 `###`**：最多三级标题
5. **硬件/嵌入式项目必须有「硬件组成」段**：列出核心芯片和模块
6. **调试/踩坑经历必写**：不要只写成功的，失败和排查过程更有价值
7. **结尾用 `>` blockquote**：个人反思和总结，不用 `## 总结` 标题（可以有大段标题，但最后一句反思用 blockquote 收尾）

---

## 写作风格

### 人称与语气

- **第一人称「我」**贯穿全文，不要用「本文」「笔者」「我们」
- 语气**自然口语化**，像在跟朋友讲自己做的东西，不要写成论文或实验报告
- **诚实写失败**：仿真跑通了实物不跑、焊错线、调了半天的 bug 原因是个低级错误——这些都要写
- **技术深度**：核心算法/电路要解释原理，但不堆砌公式和术语

### 典型句式

```
✅ "大二下学期末的电子线路课程设计，两周时间，两个项目。"
✅ "某天我突然想通了一件事：与其闷头自己瞎琢磨，不如把学到的东西写出来。"
✅ "排查了半天发现 Qco 到第二片 CPu 的跳线忘了焊——典型的'眼睛看着原理图觉得连上了，手上根本没碰那根线'。"
✅ "第一次拿烙铁的新手，前 30 个焊点还在纠结烙铁怎么拿，焊到第 80 个已经能一边聊天一边焊了。"

❌ "本文介绍了基于LM358P的LED驱动电路设计。"  （太正式）
❌ "该电路采用两级放大方案。"                      （不用"该"）
❌ "实验结果表明……"                              （不是实验报告）
❌ "本设计的创新点在于……"                          （不是论文）
```

### 技术术语

- 芯片型号、协议名、工具名**保留英文原名**：LM358P、NE555、CD40110、STM32、ESP32
- 英文术语和中文之间**不加空格**（全文统一）
- 代码/寄存器/引脚名用反引号：`P0`、`Qco`、`malloc`
- 中文技术术语可以缩写但首次出现要解释

---

## 排版格式

### 强调

- **粗体**用于关键芯片、核心概念、重要结论
- `代码`用于引脚名、寄存器、变量名、文件路径
- 不要用斜体（中文不适合斜体）

### 列表

- 无序列表用 `-`，有序列表用 `1.`
- 每个列表项以句号或分号结尾（中文列表不用标点也可，但要统一）
- 嵌套列表缩进 2 空格

### 表格

```markdown
| 引脚 | 名称 | 功能 |
|------|------|------|
| 9 (CPu) | 加法计数时钟 | 上升沿触发，计数器 +1 |
```

- 表头用中文
- 技术参数保留英文
- 表格前后各空一行

### 公式

- 用 LaTeX `$$...$$` 独立行公式
- 简单公式可以行内 `$...$`
- 不要用代码块写公式

### 代码块

- 必须指定语言：` ```c `、` ```python `、` ```js `
- 代码块前后各空一行
- 不写完整程序，只贴关键片段

### 图片

```markdown
![描述性替代文字](../assets/course-design/img_04_01.png)
```

- 路径：`../assets/` 开头（相对于 posts/ 目录）
- 图片放 `assets/` 下按项目分子目录
- alt 文字必须写清楚图片内容
- 每篇文章 3-12 张图片，选关键环节的代表性图片
- 不要连续放多张图没有文字间隔

### 分隔线

- 大段落之间用 `---`（三个横线）
- 不要用 `***` 或其他形式

---

## posts.json 规范

每篇文章在 `posts.json` 中对应一条记录：

```json
{
  "id": "kebab-case-filename",
  "title": "文章标题（吸引人、具体）",
  "date": "YYYY-MM-DD",
  "tags": ["标签1", "标签2", ...],
  "collection": "独立项目 或 从零到一物联网项目实战",
  "readTime": "大约 X 分钟",
  "excerpt": "1-2句话摘要，吸引点击，包含关键技术名词"
}
```

### 字段规则

- **id**：英文 kebab-case，与 md 文件名一致
- **title**：和 md 文件中的 `# 标题` 完全一致
- **date**：YYYY-MM-DD，反映项目实际完成或课程时间
- **tags**：英文技术名词 + 中文分类标签，通常 4-8 个
  - 技术类：C语言、Python、STM32、ESP32、51单片机、嵌入式、PCB、NFC
  - 分类类：课程设计、开发、开源、物联网、硬件、算法、数据结构
  - 芯片型号放入 tags：LM358P、NE555、CD40110
- **collection**：二选一
  - `"独立项目"` — 自己做的非课设项目、工具、学习笔记
  - `"从零到一物联网项目实战"` — 课设、嵌入式/硬件/物联网相关实践
  - 生活随笔类可以没有 collection
- **readTime**：`"大约 X 分钟"`，按 300-400 字/分钟估算
- **excerpt**：1-2 句话，包含核心技术名词，不说废话

### 排序

- 新文章放在数组**最前面**（按日期倒序）

---

## 文章分类与标签参考

### 常见标签组合

| 文章类型 | 典型标签 |
|----------|----------|
| 51单片机课设 | `["单片机", "51单片机", "嵌入式", "课程设计", "传感器"]` |
| STM32/ESP32 项目 | `["嵌入式", "物联网", "开发", "开源", "STM32", "ESP32"]` |
| 纯软件开发 | `["开发", "开源", "工具", "Python", "FastAPI"]` |
| 算法/数据结构 | `["数据结构", "C语言", "算法", "开发"]` |
| 硬件/PCB | `["PCB", "NFC", "嘉立创EDA", "硬件"]` |
| 模拟/数字电路课设 | `["课程设计", "模拟电路", "数字电路", "焊接", "芯片型号"]` |
| 生活随笔 | `["校园", "生活感悟", "随想"]` |
| 教程/工具配置 | `["嵌入式", "物联网", "开发工具"]` |

---

## 从 PDF/文档生成文章的流程

当用户提供 PDF 或文档要求生成文章时：

1. **提取内容**：阅读全文，理解项目背景、技术细节、关键数据
2. **提取图片**：从 PDF 中提取所有图片，保存到 `assets/<project-name>/`
3. **提炼叙事**：不要照搬 PDF 结构，重新组织为博客叙事——先讲动机和背景，再讲技术，最后讲收获
4. **补个人视角**：PDF 报告通常没有个人故事，需要根据技术细节合理补充调试经历和个人感受（但要基于 PDF 中已有的数据和事实）
5. **标题吸引人**：不要直接用报告标题，套用上述标题模式
6. **引用图片**：选最关键的 3-12 张，图文间隔合理
7. **更新 posts.json**：新文章加在数组最前面

---

## 禁止事项

- ❌ 标题用课程名称打头
- ❌ 用「本文」「笔者」「我们」代替「我」
- ❌ 写成实验报告/论文风格（背景→原理→步骤→结果）
- ❌ 只写成功不写失败
- ❌ 堆砌大段公式推导
- ❌ 图片连续堆放没有文字间隔
- ❌ 代码块不指定语言
- ❌ 结尾不用 blockquote 收束
- ❌ 技术术语加英文括号重复（如「发光二极管（LED）」— 直接写 LED 就好）
- ❌ 中文和英文/数字之间人为加空格
